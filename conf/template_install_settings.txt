### Installation path
INSTALL_PATH='/opt/relecov-platform'
PROJECT_NAME='relecov_platform'
REQUIRED_MODULES='core dashboard docs'
MIGRATION_MODULES='core dashboard django_plotly_dash'


### Container runtime settings
# Host directory for Apache bind-mounted configuration files.
# Keep this as a writable host path for rootless or hardened deployments.
APACHE_CONF_PATH='/srv/containers/bind/relecov-platform/relecov_apache_conf'
# Host path for the bind-mounted Django settings.py.
DJANGO_SETTINGS_PATH='/srv/containers/bind/relecov-platform/relecov_django_setting/settings.py'
# Host directory for Apache logs.
APACHE_LOG_PATH='/var/log/local/relecov-platform/apache'
# Host directory for RELECOV Platform application logs.
PLATFORM_LOG_PATH='/var/log/local/relecov-platform/apps'
# Host directory for iSkyLIMS application logs when using the integrated stack.
ISKYLIMS_LOG_PATH='/var/log/local/relecov-iskylims/apps'
# Apache virtual host names rendered into the container reverse proxy config.
RELECOV_PLATFORM_SERVER_NAME=''
RELECOV_ISKYLIMS_SERVER_NAME='relecov-iskylims.isciiides.es'
RELECOV_NEXTSTRAIN_SERVER_NAME='nextstrain.isciiides.es'
# Apache server-status virtual host and access control.
SERVER_STATUS_SERVER_NAME=''
SERVER_STATUS_ALIASES='127.0.0.1 localhost'
SERVER_STATUS_ALLOW_FROM='127.0.0.1 localhost'
# Forwarded headers set by Apache in production.
APACHE_FORWARDED_PROTO='https'
APACHE_FORWARDED_PORT='443'
# Host port published for Apache; its container port remains 8081.
APACHE_HOST_PORT='8090'
# UID/GID for the non-root relecov-platform user inside the app container.
APP_UID='1212'
APP_GID='1212'
# Shell assigned to the container runtime user during image build.
APP_SHELL='/sbin/nologin'
# Internal app ports used by Gunicorn/Nextstrain.
APP_PORT='8000'
ISKYLIMS_APP_PORT='8001'
NEXTSTRAIN_PORT='8100'
# Django production debug flag. Keep disabled in production.
DJANGO_DEBUG='false'
# Django persistent database connection lifetime in seconds.
DB_CONN_MAX_AGE='60'
# Gunicorn runtime tuning.
WEB_CONCURRENCY='2'
GUNICORN_THREADS='2'
GUNICORN_TIMEOUT='120'
GUNICORN_KEEPALIVE='5'

### (optional) Python installation path where pip and python executables are located
PYTHON_BIN_PATH='python3' # example: /opt/python/3.9.6/bin/python3

### Settings required to access database

DB_USER='<user_name>'
DB_PASS='<your_passwd>'
DB_NAME='<your_dbname>'
DB_SERVER_IP='<IP_db_server>'
DB_PORT=3306

### Settings required for accessing relecov-platform
LOCAL_SERVER_IP='<Relecov_IP>' # example: 172.0.0.1
DNS_URL='<Relecov_url_address>' # example: relecov.isciii.es
SUPERUSER='admin' # name of the django superuser that will be created

### Logs settings
LOG_TYPE="symbolic_link" # can be symbolic link, or regular_folder
LOG_PATH="" # mandatory if LOG_TYPE="symbolic_link", where is the log folder so we can create a symbolic link in the repository folder.
