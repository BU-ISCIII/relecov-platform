# Apache reverse proxy configuration customized for the integrated RELECOV stack.
# container_install.sh renders the variables below into deployment/apache.

# ============================================================================
# RELECOV PLATFORM
# ============================================================================
<VirtualHost *:8080>
    ServerName ${APACHE_SERVER_NAME}

    ProxyRequests Off
    ProxyPreserveHost On
    AllowEncodedSlashes NoDecode
    RequestHeader set X-Forwarded-Proto "${APACHE_FORWARDED_PROTO}"
    RequestHeader set X-Forwarded-Port "${APACHE_FORWARDED_PORT}"
    RequestHeader set X-Forwarded-Host "${APACHE_SERVER_NAME}"

    LimitRequestBody ${APACHE_LIMIT_REQUEST_BODY}
    <IfModule security2_module>
        SecRequestBodyLimit ${APACHE_LIMIT_REQUEST_BODY}
        SecRequestBodyNoFilesLimit ${APACHE_LIMIT_REQUEST_BODY}
    </IfModule>

    ProxyPass /static/ !
    Alias /static/ "${RELECOV_PLATFORM_INSTALL_PATH}/static/"
    <Directory "${RELECOV_PLATFORM_INSTALL_PATH}/static">
        Require all granted
    </Directory>

    ProxyPass /documents/ !
    Alias /documents/ "${RELECOV_PLATFORM_INSTALL_PATH}/documents/"
    <Directory "${RELECOV_PLATFORM_INSTALL_PATH}/documents">
        Require all granted
    </Directory>

    ProxyPass / http://relecov-platform:${RELECOV_PLATFORM_APP_PORT}/
    ProxyPassReverse / http://relecov-platform:${RELECOV_PLATFORM_APP_PORT}/
    ProxyTimeout ${APACHE_PROXY_TIMEOUT}
    TimeOut ${APACHE_PROXY_TIMEOUT}

    CustomLog logs/relecov-platform-apache.access.log combined env=!forwarded
    CustomLog logs/relecov-platform-apache.access.log proxy env=forwarded
    ErrorLog logs/relecov-platform-apache.error.log
</VirtualHost>

# ============================================================================
# iSkyLIMS
# ============================================================================
<VirtualHost *:8080>
    ServerName ${RELECOV_ISKYLIMS_SERVER_NAME}

    ProxyRequests Off
    ProxyPreserveHost On
    AllowEncodedSlashes NoDecode
    RequestHeader set X-Forwarded-Proto "${APACHE_FORWARDED_PROTO}"
    RequestHeader set X-Forwarded-Port "${APACHE_FORWARDED_PORT}"
    RequestHeader set X-Forwarded-Host "${RELECOV_ISKYLIMS_SERVER_NAME}"

    LimitRequestBody ${APACHE_LIMIT_REQUEST_BODY}
    <IfModule security2_module>
        SecRequestBodyLimit ${APACHE_LIMIT_REQUEST_BODY}
        SecRequestBodyNoFilesLimit ${APACHE_LIMIT_REQUEST_BODY}
    </IfModule>

    ProxyPass /static/ !
    Alias /static/ "${RELECOV_ISKYLIMS_INSTALL_PATH}/static/"
    <Directory "${RELECOV_ISKYLIMS_INSTALL_PATH}/static">
        Require all granted
    </Directory>

    ProxyPass /documents/ !
    Alias /documents/ "${RELECOV_ISKYLIMS_INSTALL_PATH}/documents/"
    <Directory "${RELECOV_ISKYLIMS_INSTALL_PATH}/documents">
        Require all granted
    </Directory>

    ProxyPass / http://relecov-iskylims:${RELECOV_ISKYLIMS_APP_PORT}/
    ProxyPassReverse / http://relecov-iskylims:${RELECOV_ISKYLIMS_APP_PORT}/
    ProxyTimeout ${APACHE_PROXY_TIMEOUT}
    TimeOut ${APACHE_PROXY_TIMEOUT}

    CustomLog logs/relecov-iskylims-apache.access.log combined env=!forwarded
    CustomLog logs/relecov-iskylims-apache.access.log proxy env=forwarded
    ErrorLog logs/relecov-iskylims-apache.error.log
</VirtualHost>

# ============================================================================
# Nextstrain / Auspice
# ============================================================================
<VirtualHost *:8080>
    ServerName ${RELECOV_NEXTSTRAIN_SERVER_NAME}

    ProxyRequests Off
    ProxyPreserveHost On
    AllowEncodedSlashes NoDecode
    RequestHeader set X-Forwarded-Proto "${APACHE_FORWARDED_PROTO}"
    RequestHeader set X-Forwarded-Port "${APACHE_FORWARDED_PORT}"
    RequestHeader set X-Forwarded-Host "${RELECOV_NEXTSTRAIN_SERVER_NAME}"

    ProxyPass / http://relecov-platform-nextstrain:${NEXTSTRAIN_PORT}/ nocanon
    ProxyPassReverse / http://relecov-platform-nextstrain:${NEXTSTRAIN_PORT}/
    ProxyTimeout ${APACHE_PROXY_TIMEOUT}
    TimeOut ${APACHE_PROXY_TIMEOUT}

    CustomLog logs/relecov-nextstrain-apache.access.log combined env=!forwarded
    CustomLog logs/relecov-nextstrain-apache.access.log proxy env=forwarded
    ErrorLog logs/relecov-nextstrain-apache.error.log
</VirtualHost>
